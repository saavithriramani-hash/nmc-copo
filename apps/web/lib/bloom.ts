/** Bloom's taxonomy levels for CO definitions (FR-5). Display strings; the engine does not interpret them. */
export const BLOOM_LEVELS = ['Remember', 'Understand', 'Apply', 'Analyse', 'Evaluate', 'Create'] as const;
