{
  "name": "copo",
  "private": true,
  "description": "CO-PO attainment application for Nehru Memorial College (Autonomous). See CLAUDE.md.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
